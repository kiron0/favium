# favium-cli

CLI companion for [`favium`](https://www.npmjs.com/package/favium). Converts PNG, JPEG, WebP, GIF, SVG, AVIF, HEIC, and TIFF sources into PNG/ICO favicon bundles.

```bash
npx favium-cli --source ./logo.webp --preset web-app --yes
```

Requires Node.js 22 or newer.
